language: generic

addons:
  apt:
    packages:
      - python3
      - python3-lxml
      - python3-requests

script:
- make find
- make file | tee log
# Next line checks if anything needs to be created
- test $(grep 'hub issue create' log | wc -l) -eq 0 
